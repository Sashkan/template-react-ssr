import React from 'react'
import { storiesOf } from '@storybook/react'
import { HeaderNoFilters } from 'components'

storiesOf('HeaderNoFilters', module)
  .add('default', () => (
    <HeaderNoFilters />
  ))
